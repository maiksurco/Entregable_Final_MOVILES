server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevRooster/Bolsa-Laboral.git # ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: flutter




  security:
    user:
      name: root
      password: 123456